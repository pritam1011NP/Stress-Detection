# Stress-Detection
This project detects stress levels in real time using facial landmarks and emotion recognition. Built with OpenCV, MediaPipe, and TensorFlow, it computes a stress score (0–30), displays a color-coded overlay, logs data to CSV, and generates a stress trend graph.
